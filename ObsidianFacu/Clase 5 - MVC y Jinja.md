Model View Controller
En el código en Python, el modelo va a interactuar con la DB.

Blueprint es una herramienta de Flask para generar modulos (encapsularlos). Funcionalidad para procesar rutas y reglas. El controlador va a tener distintas partes, cada una es un Blueprint

Hay que diferenciar los tags de HTML puro, de los que aparecen al usar Jinja

Id: solo sirve para el browser, para el DOM
Name: Es el parametro que llega como parámetro al que recibe el formulario

Checkpoint